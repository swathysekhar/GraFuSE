# Dataset

Place datasets here locally. Large datasets are intentionally excluded from Git.

Expected classification format:

```text
SMILES<TAB>label
```

Update the dataset path/configuration before training.
